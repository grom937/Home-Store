spring:
  datasource:
    url: jdbc:mysql://localhost:3306/homestore
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        type:
          preferred_uuid_jdbc_type: CHAR

server:
  port: 8080